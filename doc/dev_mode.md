# Run in dev mode

Use two consoles.

## Run the backend

    make run

## Run the frontend

    make run-ui

Open <http://localhost:3000> to login into the admin interface:

    xdg-open http://localhost:3000
